{
  "name": "lambda-tree",
  "version": "0.1.0",
  "description": "A lambda based HTTP batch processor",
  "main": "index.js",
  "author": "Allain Lalonde <allain.lalonde@gmail.com>",
  "license": "MIT",
  "scripts": {
    "deploy": "rm -r node_modules/ && npm install --production && sls deploy --verbose",
    "format": "standard-format --write",
    "test": "tape test/**/*.js | colortape"
  },
  "dependencies": {
    "aws-sdk": "^2.10.0",
    "dynamo-converter": "^1.0.1",
    "request": "^2.79.0",
    "serverless-offline": "^3.8.3",
    "shortid": "^2.2.6",
    "tv4": "^1.2.7"
  },
  "devDependencies": {
    "clear-require": "^2.0.0",
    "colortape": "^0.1.2",
    "tape": "^4.6.3",
    "testdouble": "^1.11.1"
  }
}
